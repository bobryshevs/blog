tags:
  - "user"
summary: "Use it to remove token pair linked with current user"
produces: ["application/json"]

security:
  - bearerAuth: []

responses:
  200:
    description: OK
  409:
    content:
      application/json:
        schema:
          $ref: "#components/schemas/Error"
